# 默认不配置 VITE_API_BASE_URL。
# 前端会请求同域 /api：
# - 本地开发：vite.config.ts proxy 到 http://localhost:5111
# - 生产部署：Nginx proxy /api 到后端服务
#
# 只有当前端和后端必须跨域部署时才打开下面这一行：
# VITE_API_BASE_URL=https://api.example.com
